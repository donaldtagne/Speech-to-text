# Voice-Enabled Retrieval Augmented Generation

This project demonstrates how automatic speech recognition using Whisper can be combined with a retrieval augmented generation (RAG) workflow. Users record a question, the speech is transcribed into text, and the text is answered by conditioning a small language model on topically relevant documents from the knowledge base.
